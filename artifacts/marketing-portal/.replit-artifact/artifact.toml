kind = "web"
previewPath = "/"
title = "Smallbiz Marketing Portal"
version = "1.0.0"
id = "artifacts/marketing-portal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20229

[services.development]
run = "pnpm --filter @workspace/marketing-portal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/marketing-portal", "run", "build" ]
publicDir = "artifacts/marketing-portal/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20229"
BASE_PATH = "/"
